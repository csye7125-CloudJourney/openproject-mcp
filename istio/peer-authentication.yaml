---
# STRICT mTLS for the openproject-mcp namespace. Plaintext peers are rejected.
apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: default
  namespace: openproject-mcp
spec:
  mtls:
    mode: STRICT
