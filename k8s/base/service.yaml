apiVersion: v1
kind: Service
metadata:
  name: openproject-mcp
  labels:
    app.kubernetes.io/name: openproject-mcp
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: openproject-mcp
