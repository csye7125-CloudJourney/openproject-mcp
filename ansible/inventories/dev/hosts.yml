---
# Static inventory skeleton for dev. Real hosts arrive via the aws_ec2
# dynamic plugin (aws_ec2.yml) once terraform spins up the instances.
# This file just exists so playbooks have groups to bind to in dry-runs.

all:
  children:
    bastions:
      hosts: {}
    mcp_workers:
      hosts: {}
